import Foundation
import SwitcherCore

var passed = 0
var failed = 0
var results: [(String, Bool)] = []

func test(_ label: String, _ block: () -> Bool) {
    let ok = block()
    results.append((label, ok))
    if ok { passed += 1 } else { failed += 1 }
}

let dict = WordDictionary()
let c = LayoutConverter(dictionary: dict)
let dictConv = LayoutConverter(dictionary: dict)

print("╔══════════════════════════════════════════════════════════╗")
print("║              RSW — полный набор тестов                   ║")
print("╚══════════════════════════════════════════════════════════╝")
print("")

// ────────────────────────────────────────────────────────
// 1. Известные слова в неверной раскладке
// ────────────────────────────────────────────────────────
print("━━━ 1. Известные пары конвертации ━━━")

let knownPairs: [(typed: String, expected: String)] = [
    ("ghbdtn", "привет"),
    ("руддщ", "hello"),
    ("еуые", "test"),
    ("ьфс", "mac"),
]

for p in knownPairs {
    test("'\(p.typed)' → '\(p.expected)'") {
        c.convert(p.typed)?.text == p.expected
    }
}

// ────────────────────────────────────────────────────────
// 2. Принудительная конвертация работает для любого слова
// ────────────────────────────────────────────────────────
print("━━━ 2. Принудительная конвертация ━━━")

let forcePairs: [(typed: String, expected: String, lang: KeyboardLanguage)] = [
    ("hello", "руддщ", .russian),
    ("привет", "ghbdtn", .english),
]

for p in forcePairs {
    test("ПРИНУДИТЕЛЬНО '\(p.typed)' → '\(p.expected)' (\(p.lang))") {
        let r = c.forceConvert(p.typed)
        return r?.text == p.expected && r?.language == p.lang
    }
}

// ────────────────────────────────────────────────────────
// 3. Короткие слова не должны исправляться автоматически
// ────────────────────────────────────────────────────────
print("━━━ 3. Короткие слова отклоняются ━━━")

let shortWords = [
    "ab", "no", "ok", "hi", "we", "do", "am", "is", "it", "in",
    "йц", "фв", "яч", "ые", "уе", "му", "ри", "ти", "но", "на",
    "q", "й", "a", "z", "w", "ц",
]

for w in shortWords {
    test("КОРОТКОЕ '\(w)' → nil") {
        c.convert(w) == nil
    }
}

// ────────────────────────────────────────────────────────
// 4. Смешанные языки не должны исправляться
// ────────────────────────────────────────────────────────
print("━━━ 4. Смешанный ввод отклоняется ━━━")

let mixedWords = [
    "helloпривет", "abcйцук", "testтест", "мирworld",
    "abc123", "hello1", "test!", "hello?", "мир!",
]

for w in mixedWords {
    test("СМЕШАННОЕ '\(w)' → nil") {
        c.convert(w) == nil
    }
}

// ────────────────────────────────────────────────────────
// 5. Пустые строки и пробелы
// ────────────────────────────────────────────────────────
print("━━━ 5. Пустой ввод отклоняется ━━━")

let emptyWords = ["", " ", "\n", "\t", "  "]
for w in emptyWords {
    test("ПУСТОЕ '\(w.replacingOccurrences(of: "\n", with: "\\n"))' → nil") {
        c.convert(w) == nil
    }
}

// ────────────────────────────────────────────────────────
// 6. Корректные английские слова сохраняются
// ────────────────────────────────────────────────────────
print("━━━ 6. Корректный английский ввод сохраняется ━━━")

let correctEn = [
    "hello", "world", "the", "is", "it", "in", "on", "at", "to", "of",
    "and", "an", "a", "my", "we", "he", "she", "they", "do", "no",
    "so", "if", "me", "up", "us", "be", "as", "am", "or", "by",
    "go", "ok", "hi", "this", "that", "with", "from", "have", "been",
    "will", "would", "could", "should", "can", "may", "shall",
    "not", "but", "what", "when", "where", "which", "who", "how",
    "all", "each", "every", "both", "few", "more", "most", "other",
    "shall", "must", "need", "dare", "ought",
]

for w in correctEn {
    test("EN сохраняется '\(w)'") {
        c.convert(w) == nil
    }
}

// ────────────────────────────────────────────────────────
// 7. Корректные русские слова сохраняются
// ────────────────────────────────────────────────────────
print("━━━ 7. Корректный русский ввод сохраняется ━━━")

let correctRu = [
    "привет", "мир", "и", "в", "не", "на", "я", "что", "он", "с",
    "а", "это", "как", "все", "она", "так", "его", "но", "да", "ты",
    "к", "у", "же", "вы", "за", "по", "из", "о", "от", "до",
    "ли", "нет", "вот", "ну", "уж", "бы", "ей", "их", "сам", "уже",
    "или", "ни", "если", "там", "где", "при", "над", "тоже",
    "ему", "вас", "им", "нам", "ним", "нас", "вас", "вам", "нам",
    "вдруг", "вместе", "вместо", "вниз", "вверх", "внутри",
    "вне", "под", "перед", "после", "между", "через", "около",
    "вокруг", "вдоль", "поперёк", "направо", "налево", "назад", "вперёд",
    "структуру", "будут", "буфер", "машины", "манипулятора",
]

for w in correctRu {
    test("RU сохраняется '\(w)'") {
        c.convert(w) == nil
    }
}

// ────────────────────────────────────────────────────────
// 8. Регистр сохраняется при принудительной конвертации
// ────────────────────────────────────────────────────────
print("━━━ 8. Сохранение регистра ━━━")

test("ПРИНУДИТЕЛЬНО верхний регистр 'GHBDTN' → 'ПРИВЕТ'") {
    c.forceConvert("GHBDTN")?.text == "ПРИВЕТ"
}
test("ПРИНУДИТЕЛЬНО первая заглавная 'Ghbdtn' → 'Привет'") {
    c.forceConvert("Ghbdtn")?.text == "Привет"
}
test("ПРИНУДИТЕЛЬНО нижний регистр 'ghbdtn' → 'привет'") {
    c.forceConvert("ghbdtn")?.text == "привет"
}

// ────────────────────────────────────────────────────────
// 9. Точность таблицы символов
// ────────────────────────────────────────────────────────
print("━━━ 9. Таблица символов ━━━")

let mapPairs: [(en: String, ru: String)] = [
    ("q", "й"), ("w", "ц"), ("e", "у"), ("r", "к"), ("t", "е"),
    ("y", "н"), ("u", "г"), ("i", "ш"), ("o", "щ"), ("p", "з"),
    ("a", "ф"), ("s", "ы"), ("d", "в"), ("f", "а"), ("g", "п"),
    ("h", "р"), ("j", "о"), ("k", "л"), ("l", "д"), ("z", "я"),
    ("x", "ч"), ("c", "с"), ("v", "м"), ("b", "и"), ("n", "т"),
    ("m", "ь"),
]

for p in mapPairs {
    test("ТАБЛИЦА EN→RU '\(p.en)' → '\(p.ru)'") {
        c.forceConvert(p.en)?.text == p.ru
    }
    test("ТАБЛИЦА RU→EN '\(p.ru)' → '\(p.en)'") {
        c.forceConvert(p.ru)?.text == p.en
    }
}

// ────────────────────────────────────────────────────────
// 10. Определение русских символов
// ────────────────────────────────────────────────────────
print("━━━ 10. Определение русских символов ━━━")

let ruChars: [(c: Character, expect: Bool)] = [
    ("а", true), ("я", true), ("ё", true), ("й", true), ("ь", true),
    ("a", false), ("z", false), ("0", false), (" ", false), ("!", false),
]

for p in ruChars {
    test("русский символ '\(p.c)' = \(p.expect)") {
        LayoutConverter.isRussianCharacter(p.c) == p.expect
    }
}

// ────────────────────────────────────────────────────────
// 11. Интеграция со словарём
// ────────────────────────────────────────────────────────
print("━━━ 11. Словарь ━━━")

dict.add("тестовое", language: .russian)
dict.add("customword", language: .english)

test("СЛОВАРЬ знает RU 'тестовое'") { dict.isKnown("тестовое", language: .russian) }
test("СЛОВАРЬ знает EN 'customword'") { dict.isKnown("customword", language: .english) }
test("СЛОВАРЬ не знает 'xyz'") { !dict.isKnown("xyz", language: .russian) }

// ────────────────────────────────────────────────────────
// 12. Разница между convert() и forceConvert()
// ────────────────────────────────────────────────────────
print("━━━ 12. convert и forceConvert ━━━")

test("convert('hello') = nil (корректное слово)") { c.convert("hello") == nil }
test("forceConvert('hello') != nil (принудительно)") { c.forceConvert("hello") != nil }
test("convert('привет') = nil (корректное слово)") { c.convert("привет") == nil }
test("forceConvert('привет') != nil (принудительно)") { c.forceConvert("привет") != nil }

// ────────────────────────────────────────────────────────
// 13. Граничные случаи: цифры в словах
// ────────────────────────────────────────────────────────
print("━━━ 13. Цифры в словах ━━━")

test("ЦИФРЫ 'hello1' → nil") { c.convert("hello1") == nil }
test("ЦИФРЫ '123abc' → nil") { c.convert("123abc") == nil }
test("ЦИФРЫ 'abc123' → nil") { c.convert("abc123") == nil }

// ────────────────────────────────────────────────────────
// 14. Пунктуация в словах
// ────────────────────────────────────────────────────────
print("━━━ 14. Пунктуация ━━━")

test("ПУНКТУАЦИЯ 'hello!' → nil") { c.convert("hello!") == nil }
test("ПУНКТУАЦИЯ 'test@' → nil") { c.convert("test@") == nil }
test("ПУНКТУАЦИЯ 'привет.' → nil") { c.convert("привет.") == nil }

// ────────────────────────────────────────────────────────
// 15. Safety-регрессия v0.2.17: false positives из логов
//     Корректные русские слова с неоднозначными буквами
//     (`й`, `ы`, `э`, `ю`, `ё`, `ъ`, `ь`, `щ`, `ц`)
//     не должны конвертироваться в английскую раскладку.
// ────────────────────────────────────────────────────────
print("━━━ 15. Safety-регрессия: FP русских слов ━━━")

let falsePositivesRu: [(typed: String, note: String)] = [
    ("структуру", "содержит `ь`, `у`, `к` — встречается в RU-логах"),
    ("будут", "содержит `у`, `д` — встречается в RU-логах"),
    ("буфер", "содержит `у`, `ф`, `е`, `р` — встречается в RU-логах"),
    ("машины", "содержит `ы`, `ш`, `и` — встречается в RU-логах"),
    ("манипулятора", "длинное слово с `ы`, `у`, `л`, `я` — встречается в RU-логах"),
    ("привет", "эталон — никогда не должен конвертироваться в EN"),
    ("вдруг", "содержит `д`, `р`, `у` — выглядит неоднозначно"),
    ("вокруг", "содержит `о`, `к`, `р`, `у` — выглядит неоднозначно"),
    ("вдоль", "содержит `д`, `о`, `л`, `ь` — выглядит неоднозначно"),
    ("направо", "содержит `п`, `р`, `а`, `в`, `о` — выглядит неоднозначно"),
    ("вперёд", "содержит `ё` — особый символ"),
    ("тоже", "короткое слово с `о`, `ж`, `е`"),
    ("ыфсщ", "5 букв с `ы`, `ф`, `с`, `щ` — выглядит неоднозначно"),
]

for p in falsePositivesRu {
    test("FP-RU: '\(p.typed)' → nil (\(p.note))") {
        c.convert(p.typed) == nil
    }
}

// ────────────────────────────────────────────────────────
// 16. Safety-регрессия v0.2.17: true positives из логов
//     Заведомо неправильная раскладка должна конвертироваться.
// ────────────────────────────────────────────────────────
print("━━━ 16. Safety-регрессия: TP пар из логов ━━━")

let truePositives: [(typed: String, expected: String)] = [
    ("ghbdtn", "привет"),
    ("руддщ", "hello"),
    ("еуые", "test"),
    ("ьфс", "mac"),
]

for p in truePositives {
    test("TP: '\(p.typed)' → '\(p.expected)'") {
        c.convert(p.typed)?.text == p.expected
    }
}

// ────────────────────────────────────────────────────────
// 17. Safety-регрессия v0.2.17: известные EN-слова в словаре
//     `hello`, `world`, `test`, `mac` уже в built-in EN dict,
//     должны возвращать nil из convert() и forceConvert не должен
//     искажать их.
// ────────────────────────────────────────────────────────
print("━━━ 17. Safety-регрессия: built-in EN dict ━━━")

test("DICT-EN: 'hello' не конвертируется") { c.convert("hello") == nil }
test("DICT-EN: 'world' не конвертируется") { c.convert("world") == nil }
test("DICT-EN: 'test' не конвертируется") { c.convert("test") == nil }
test("DICT-EN: 'mac' не конвертируется") { c.convert("mac") == nil }
test("DICT-EN: 'switch' не конвертируется") { c.convert("switch") == nil }
test("DICT-EN: 'buffer' не конвертируется") { c.convert("buffer") == nil }
test("DICT-EN: 'keyboard' не конвертируется") { c.convert("keyboard") == nil }

test("DICT-RU: 'структуру' не конвертируется") { c.convert("структуру") == nil }
test("DICT-RU: 'будут' не конвертируется") { c.convert("будут") == nil }
test("DICT-RU: 'буфер' не конвертируется") { c.convert("буфер") == nil }
test("DICT-RU: 'машины' не конвертируется") { c.convert("машины") == nil }
test("DICT-RU: 'манипулятора' не конвертируется") { c.convert("манипулятора") == nil }

// ────────────────────────────────────────────────────────
// Отчёт
// ────────────────────────────────────────────────────────

let total = passed + failed
let rate = total > 0 ? passed * 100 / total : 0

print("")
print("╔══════════════════════════════════════════════════════════╗")
print("║                     ОТЧЁТ ТЕСТОВ                        ║")
print("╚══════════════════════════════════════════════════════════╝")
print("")
print("  Всего:       \(total)")
print("  Успешно:     \(passed)")
print("  Провалено:   \(failed)")
print("  Успешность:  \(rate)%")
print("")

if failed > 0 {
    print("━━━ ПРОВАЛЕННЫЕ ТЕСТЫ ━━━")
    for (label, ok) in results where !ok {
        print("  ✗ \(label)")
    }
    print("")
}

print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
print("Завершено: \(ISO8601DateFormatter().string(from: Date()))")
print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")

if failed > 0 { exit(1) }
